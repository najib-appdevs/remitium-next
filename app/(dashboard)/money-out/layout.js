export const metadata = {
  title: "Money Out | Remitium",
  description: "Send money to any bank account, mobile wallet, or for cash pickup globally.",
};

export default function MoneyOutLayout({ children }) {
  return <>{children}</>;
}
