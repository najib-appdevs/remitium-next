export const metadata = {
  title: "KYC Verification | Remitium",
  description: "KYC verification management and tracking.",
};

export default function KYCVerificationLayout({ children }) {
  return <>{children}</>;
}
