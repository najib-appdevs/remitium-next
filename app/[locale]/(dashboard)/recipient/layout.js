export const metadata = {
  title: "Recipient | Remitium",
  description: "Recipient management and tracking.",
};

export default function RecipientLayout({ children }) {
  return <>{children}</>;
}
