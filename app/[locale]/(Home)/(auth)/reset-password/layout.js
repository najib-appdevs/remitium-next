export const metadata = {
    title: "Reset Password | Remitium",
    description: "Reset your password with Remitium to send money globally with ease.",
};

export default function ResetPasswordLayout({ children }) {
    return children;
}
